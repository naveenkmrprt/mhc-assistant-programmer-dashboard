[build]
  command = "npm run build -- --configuration production"
  publish = "dist/frontend/browser"
  # Only rebuild if files inside frontend/ changed - saves build minutes
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- frontend/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
